{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug with Live Server",
        "type": "pwa-chrome",
        "request": "launch",
        "url": "http://127.0.0.1:5500",   // ← use the exact port Live Server shows in the status bar
        "webRoot": "${workspaceFolder}"
      }
    ]
  }